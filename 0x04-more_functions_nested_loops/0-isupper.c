#include "main.h"
/**
 * _isupper - start
 * @c: argument
 * Return: return 0
 */
int _isupper(int c)
{
	char i;

	for (i = 48; 1 < 57; i++)
	{
		if (c == i)
			_putchar('1');
	}
	_putchar('0');
	return (0);
}
